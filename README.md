# 💬 Chat Room with Server (All-in-one Guide)

> Single-file guide: project overview, how to run it locally, how to push to GitHub, troubleshooting steps and useful extras — all in one place.

---

## 📂 Project files (in this folder)
- `Client.py` — Client GUI (Tkinter) + socket code  
- `Server_TCP.py` — TCP server (accepts clients, broadcasts messages)  
- `README.md` — (this file) full step-by-step instructions  
- Optional: `.gitignore` (see bottom of this file for sample)

---

## ✅ Prerequisites (Windows)
- Python 3.x installed (you have Python 3.13.5)  
- Git installed and configured (username/email)

Open **Command Prompt** (Win+R → `cmd`) for running the commands below.

---

## 1) Run the server & clients (step-by-step)

1. Open a Command Prompt and change to your project folder:
   ```bash
   cd "C:\Users\Arun\OneDrive\Documents\PROJECTS_IMP\Chat_room-with-server.-main"
